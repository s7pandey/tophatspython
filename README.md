# TopHat Python

Using Selenium (Python) and its Firefox driver, this repo exists to scrape through certain universities' websites for information on the classes that the unviersity offered including: the class sizes, the professors and the types of classes.
